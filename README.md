# JANUS-LAPIS v0.1.5 — Birth-Gate Edition

A modern GPT-guided search space for the real functions behind the philosopher's stone.

## Principle

The alchemists were not fools. They lacked instruments.
We do not worship symbols. We test functions.

## v0.1.5

This version adds the Birth-Gate decision chain: a candidate must pass the scene before it can enter champions.

```text
The stone is not only a material.
The stone is the clean scene that lets a new game appear.
The stone is also the gate that decides whether a hypothesis is ready to be born.
The stone is also the engine that changes the search space.
```

## Archetypes

Material layer:

- `LAPIS_UNIVERSAL`
- `LAPIS_CATALYST`
- `LAPIS_PURIFIER`
- `LAPIS_HEALER`
- `LAPIS_STONE`
- `LAPIS_LIFE`
- `LAPIS_ENERGY`
- `LAPIS_BIOMINERAL`
- `LAPIS_LOWHAZARD`

Scene-preparation layer:

- `LAPIS_CANVAS`
- `LAPIS_STAGEKEEPER`

Meta layer:

- `LAPIS_DEMIURGE`
- `LAPIS_WORLD_REWRITER`

## Science boundary

- Not a claim of literal transmutation.
- Not an elixir claim.
- No hazardous synthesis protocol.
- Not a certified material claim.
- No instructions for dangerous chemistry.
- All outputs are computational research vectors and expert-review requests, not measured or validated materials.

## Run

```bash
RUN_JANUS_LAPIS.bat
```

Output:

```text
_JanusLapis_RUNNER\janus_lapis_runs\SEND_TO_REVIEWERS_JANUS_LAPIS.zip
```

Main files:

```text
janus_lapis_summary.json
janus_lapis_champions.csv
janus_lapis_all_archetypes.csv
lab_request\JANUS_LAPIS_EXTERNAL_RESEARCH_BRIEF.md
lab_request\janus_lapis_external_research_request.csv
docs\JANUS_RESPONSE_INTERPRETATION.md
```

---

# Research branch — JANUS-LAPIS v0.3.3 Reverse-Gate

The historical Birth-Gate work remains intact. The active branch `research/reverse-gate-v020` hosts **JANUS 113.8 — Provenance-Aware Structured Preimage Reconstruction**.

Surface SHA conversions (audio/bitmap/Base-N/JSON/etc.) were retired after controls showed that they preserve the digest but do not reveal the source. Later whole-object / whole-file transfer attempts that produced no exact witness were also retired and frozen only as negative evidence.

## Active question

```text
TARGET SHA-256
    +
Meta Registry schema / provenance / genealogy / neighboring metadata
    ↓
Coherence Hold
    ↓
source hypotheses
    ↓
SHA256(candidate bytes)
    ↓
EXACT witness only
```

JANUS 113.8 mapping:

```text
/wormhole             -> target + allowed structural context
Chaos Spike           -> unresolved source uncertainty
Coherence Hold        -> competing preimage hypotheses
Entropy Graveyard     -> exact-hash failures / retired methods
Decoherence Collapse  -> SHA256(candidate) == target
Hippocampus           -> learned templates + recovered exact truths
Ouroboros              -> deterministic self-tests / integrity gates
```

## Current frozen result

On **47 real cryptographically verified string↔SHA challenges** discovered inside `Hawkar-usls/janus-meta-registry`:

```text
exact hash challenges recovered:  11 / 47
unique plaintexts recovered:       6
recovery rate:                     23.40%
```

Working methods:

```text
schema_song_identifier            6 hash witnesses
explicit_provenance_extraction    2 hash witnesses
registry_serialization_genealogy  3 hash witnesses
```

Examples of unique recovered source strings include:

```text
SNAP! — The Power
ERA — Ameno | Era I | 1996
R. Kelly — I Believe I Can Fly | Space Jam | 1996
The Alan Parsons Project — Sirius | Eye In The Sky | 1982
```

Two additional unique Solstice Relay representations were recovered through an alternate reversible sibling serialization and remain classified as **redundancy-assisted**.

A reconstruction is admitted only when the hidden candidate is generated from allowed side information and recomputes to the exact recorded SHA-256. Similarity, Hamming distance and aesthetically plausible text do not count.

## Gate state

```text
SHA surface-format conversion                   = RETIRED
structured context -> hidden short plaintext    = ADMITTED IN RESTRICTED REGISTRY FAMILY
explicit provenance -> hidden plaintext         = ADMITTED WITH SIDE-INFORMATION LABEL
whole-object schema projection                  = RETIRED_NEGATIVE
whole-file genealogy / companion transfer       = RETIRED_NEGATIVE
SHA -> arbitrary plaintext                      = NOT ADMITTED
SHA -> arbitrary complete file                  = NOT ADMITTED
GENERAL SHA-256 INVERSION                       = NOT CLAIMED
```

Active files:

```text
reverse_gate/janus_preimage_reconstruction.py
reverse_gate/janus_preimage_reconstruction_v2.py
reverse_gate/janus_preimage_reconstruction_v3.py
reverse_gate/janus_preimage_reconstruction_v4.py
.github/workflows/reverse-gate-preimage-v4.yml
```

Evidence / Graveyard:

```text
reverse_gate/FIRST_PREIMAGE_RECONSTRUCTION_WITNESS_2026-08-07.md
reverse_gate/NEGATIVE_WHOLE_OBJECT_GATES_V4_V5_2026-08-07.md
reverse_gate/NEGATIVE_WHOLE_FILE_GATES_V6_V7_2026-08-07.md
```

## Scientific boundary

This is **constrained preimage reconstruction with side information**, not a universal inverse for SHA-256. SHA-256 remains preimage-resistant; the working result comes from shrinking the source hypothesis space using verifiable structure and provenance, with SHA-256 serving as the exact witness gate.
